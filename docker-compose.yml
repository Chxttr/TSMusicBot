services:
  tsmusicbot:
    build: https://github.com/Kobevives/TSMusicBot.git
    container_name: tsmusicbot
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./local-config:/app/config
      - ./local-data:/app/data
      - ./local-logs:/app/logs
